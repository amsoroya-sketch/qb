# ============================================
# Backend Environment Variables - Example
# ============================================
#
# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace all placeholder values with real secrets
# 3. NEVER commit the .env file to version control
# 4. Use the secret generation commands below
#
# Generate secure secrets:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# ============================================

# ============================================
# Database
# ============================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@localhost:5433/arq_dev

# ============================================
# JWT Authentication
# ============================================
# Main JWT secret (256-bit recommended)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_generate_with_crypto_randomBytes_32

# Access token secret (for short-lived tokens)
ACCESS_TOKEN_SECRET=your_access_token_secret_here_generate_with_crypto_randomBytes_32

# Refresh token secret (for long-lived tokens)
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here_generate_with_crypto_randomBytes_32

# Alternative JWT secrets (if using different naming convention)
JWT_ACCESS_SECRET=your_jwt_access_secret_here_same_as_ACCESS_TOKEN_SECRET
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_same_as_REFRESH_TOKEN_SECRET

# Token expiration times
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_EXPIRATION=7d

# ============================================
# Redis (Session Store & Cache)
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6380
# IMPORTANT: Set a strong Redis password
REDIS_PASSWORD=your_redis_password_here

# ============================================
# Application Configuration
# ============================================
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3005

# ============================================
# Email Service (SendGrid)
# ============================================
# Get your API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# ============================================
# AWS S3 (File Storage)
# ============================================
# Get credentials from AWS IAM Console
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_REGION=us-east-1
AWS_S3_BUCKET=your-s3-bucket-name

# ============================================
# Optional: Logging & Monitoring
# ============================================
# LOG_LEVEL=info
# SENTRY_DSN=your_sentry_dsn_here

# ============================================
# Optional: Rate Limiting
# ============================================
# RATE_LIMIT_TTL=60
# RATE_LIMIT_MAX=100

# ============================================
# Optional: CORS Configuration
# ============================================
# CORS_ORIGINS=http://localhost:3000,http://localhost:3005

# ============================================
# Optional: Database Connection Pool
# ============================================
# DATABASE_POOL_MIN=2
# DATABASE_POOL_MAX=10

# ============================================
# Security Notes:
# ============================================
# - Never commit this file with real values
# - Use different secrets for dev/staging/prod
# - Rotate secrets quarterly
# - Use environment variable injection in production
# - Consider using a secret manager (AWS Secrets Manager, Vault)
